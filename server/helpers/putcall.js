/*const { mysqlConnection } = require('./server/sql/sql');

function putreq(data, id) {
    const script = 'UPDATE recipes set recipe_rating = (?) where recipe_id = (?)';
    const values = [data, id];
    return connection.query(script, values);
};
*/
